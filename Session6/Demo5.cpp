#include <stdio.h>
int main(){
	int count = 0;
	// can kiem tra so 9 co phai so nguyen to hay ko
	for(int i=1;i<=9;i++){
		if(9%i==0){
		count ++;
		}
	}
	if(count == 2){
		// moi chac chan dc no la nguyen to
	}
}
